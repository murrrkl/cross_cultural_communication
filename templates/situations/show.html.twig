{% extends 'base.html.twig' %}

{% block title %}Situations{% endblock %}

{% block body %}
    <h1>Situations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ situation.number }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ situation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('situations_index') }}">back to list</a>

    <a href="{{ path('situations_edit', {'id': situation.id}) }}">edit</a>

    {{ include('situations/_delete_form.html.twig') }}
{% endblock %}
